syntax = "proto3";

package dungeons;

service Dungeons {
    rpc reportPosition (Position) returns (EmptyResponse) {}
}

message Position {
    float lat = 1;
    float long = 2;
}

message EmptyResponse {
}
